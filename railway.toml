[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile.api"

[deploy]
startCommand = "sh -c 'cd /app/backend && uvicorn server:app --host 0.0.0.0 --port 8000'"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
